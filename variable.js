'use strict'
let name;
console.log(name)

// assigning variable later;
name = "Hello"
console.log(name)

// varible naming rules
// can contain digit, letters, $ , _
// must start with letter, $ or _
// cant use reserved keywords like this, function
// variables are case sensitive
// length of the variable can be as much as you can
// it can follow camelCaseWay or underscore_way